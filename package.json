{
  "name": "stratara",
  "version": "0.0.1",
  "private": true,
  "description": "Stratara (Project Atlas) — AI-powered commercial expansion intelligence platform. Monorepo root.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:server": "npm run dev --workspace server",
    "dev:client": "npm run dev --workspace client",
    "build:client": "npm run build --workspace client"
  }
}
